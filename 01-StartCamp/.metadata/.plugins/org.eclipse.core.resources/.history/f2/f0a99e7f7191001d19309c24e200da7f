//단축키 ctrl + m 눌러보자 / 패키지 익스플로러 창 여닫는 단축키임
		/** 깃에 소스코드를 올릴때 신경을 쓰자. 지금 작성하고 있는 주석문이 있어야한다.
		 * 소스에 일관성이 있어야하고, 중복이 없어야한다.
		 * 소스에 철학이 있어야한다. 나만의 틀이 철학이다.
		 * 동일한 규칙을 가지고 작성해야한다는 것임
		 * 웹에 있는 남의 것을 그대로 따온 느낌이 들지 않도록
		 * 이 주석문을 문서화 주석이라고 한다.
		 * 어떤 클래스인지 설명해주는 것
		 * 웹에서 보는 주석문이 이 설명글을 보여주는 것임
		 * 내가 짠 코드를 문서화 시킬때 사용함
		 * 깃에 만약 코드를 올린다면 최소한 .java파일에 이 주석문이 있어야함
		 * 수업때는 시간 상 매번 사용하지는 않을 것
		 */

package com.ssafy.startcamp;

/**
 * 
 * @author SSAFY
 *
 */
public class Test01 {
	
	/**
	 * 
	 */
	public String msg;
	
	/**
	 * 
	 */
	public void test() {}
	
	
	public static void main(String[] args) {
		System.out.println();
	}
}
